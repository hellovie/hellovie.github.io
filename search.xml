<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Git 学习笔记</title>
      <link href="/archives/3c3cdb74.html"/>
      <url>/archives/3c3cdb74.html</url>
      
        <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>最近用 GitHub 搭建了个人博客，顺便再回顾一下之前了解过但是没熟练掌握的 Git ，本篇主要是记录关于 Git 的一些命令和技巧，详细教程可以阅读<a href="https://www.liaoxuefeng.com/wiki/896043488029600" title="廖雪峰Git教程">廖雪峰Git教程</a>。</p><p><strong>git 新更改：</strong> <code>git checkout</code> 可替换命令 <code>git switch</code> 和 <code>git restore</code> ，这里的笔记以新更改为主。</p><br><h3 id="基本配置"><a href="#基本配置" class="headerlink" title="基本配置"></a>基本配置</h3><blockquote><p>–global：设置全局变量，局部变量则无需 <code>--global</code> 参数。</p></blockquote><h4 id="设置用户信息"><a href="#设置用户信息" class="headerlink" title="设置用户信息"></a>设置用户信息</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.name <span class="string">&quot;用户名&quot;</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email <span class="string">&quot;用户邮箱&quot;</span></span></span><br></pre></td></tr></table></figure><h4 id="查看配置信息"><a href="#查看配置信息" class="headerlink" title="查看配置信息"></a>查看配置信息</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.name</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email</span></span><br></pre></td></tr></table></figure><br><h3 id="Git-基础操作"><a href="#Git-基础操作" class="headerlink" title="Git 基础操作"></a>Git 基础操作</h3><h4 id="初始化-Git-仓库"><a href="#初始化-Git-仓库" class="headerlink" title="初始化 Git 仓库"></a>初始化 Git 仓库</h4><blockquote><p><strong>工作区(workspace)：</strong></p><ul><li><strong>未跟踪(untracked)：</strong>新创建文件。</li><li><strong>未暂存(unstaged)：</strong>修改已有文件。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git init</span></span><br></pre></td></tr></table></figure><h4 id="把文件添加到仓库"><a href="#把文件添加到仓库" class="headerlink" title="把文件添加到仓库"></a>把文件添加到仓库</h4><blockquote><p><strong>工作区(workspace) –&gt; 暂存区(index)</strong></p><p><strong>已暂存(staged)：</strong>提交到仓库之前的缓存区。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git add &lt;file&gt;</span></span><br></pre></td></tr></table></figure><h4 id="把文件提交到仓库"><a href="#把文件提交到仓库" class="headerlink" title="把文件提交到仓库"></a>把文件提交到仓库</h4><blockquote><p><strong>暂存区(index) –&gt; 本地仓库(repository)</strong></p><p><strong>说明：</strong>如果把文件提交到仓库后，工作区(workspace)无操作，则暂存区(index)是干净的。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git commit -m <span class="string">&quot;本次提交的说明&quot;</span></span></span><br></pre></td></tr></table></figure><h4 id="查看文件状态"><a href="#查看文件状态" class="headerlink" title="查看文件状态"></a>查看文件状态</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git status</span></span><br></pre></td></tr></table></figure><h4 id="查看文件不同"><a href="#查看文件不同" class="headerlink" title="查看文件不同"></a>查看文件不同</h4><blockquote><ol><li><code>git diff</code>：是查看 working tree 与 index 的差别的。</li><li><code>git diff --cached</code>：是查看 index 与 repository 的差别的。</li><li><code>git diff HEAD</code>：是查看 working tree 和 repository 的差别的。其中：HEAD 代表的是最近的一次 commit 的信息。</li></ol></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git diff &lt;file&gt;</span></span><br></pre></td></tr></table></figure><h4 id="显示提交日志"><a href="#显示提交日志" class="headerlink" title="显示提交日志"></a>显示提交日志</h4><blockquote><p><strong>options</strong>：</p><ul><li><code>--all</code>：显示所有分支。</li><li><code>--pretty=oneline</code>：将提交信息显示为一行。</li><li><code>--abbrev-commit</code>：使得输出的commit更简短。</li><li><code>--graph</code>：以图的形式显示。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span> [options]</span></span><br></pre></td></tr></table></figure><h4 id="查看历史命令"><a href="#查看历史命令" class="headerlink" title="查看历史命令"></a>查看历史命令</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reflog</span></span><br></pre></td></tr></table></figure><h4 id="撤销修改"><a href="#撤销修改" class="headerlink" title="撤销修改"></a>撤销修改</h4><blockquote><p><strong>无参：</strong>撤销工作区(workspace)的修改。</p><p><code>--staged</code>：撤销上传到暂存区(index)。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git restore [--staged] &lt;file&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="回退版本"><a href="#回退版本" class="headerlink" title="回退版本"></a>回退版本</h3><h4 id="回退上个版本"><a href="#回退上个版本" class="headerlink" title="回退上个版本"></a>回退上个版本</h4><blockquote><p>回退多个版本可以把 <code>^</code> 换成 <code>~&lt;回退版本数&gt;</code>。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard HEAD^</span></span><br></pre></td></tr></table></figure><h4 id="回退到指定版本"><a href="#回退到指定版本" class="headerlink" title="回退到指定版本"></a>回退到指定版本</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard &lt;commit id&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="删除文件"><a href="#删除文件" class="headerlink" title="删除文件"></a>删除文件</h3><blockquote><p><strong>注意：从来没有被添加到版本库就被删除的文件，是无法恢复的！</strong></p><p><strong>文件修改后，与版本库不同时，再删除，此时options：</strong></p><ul><li><p><code>--cached</code>：仅删除暂存区里的文件。此时 commit ，会出现工作区存在修改后的删除文件，而版本库此文件被删除。</p></li><li><p><code>-f </code>：强制删除[工作区+暂存区]。此时 commit ，会出现工作区和版本库此文件都删除。</p></li></ul><p><strong>理解：</strong></p><ol><li><p><code>rm &lt;file&gt;</code> 可以在任何文件夹都可以用，包括不是git仓库的地方，是用来删除文件。如果用在git仓库里，则可以视为<strong>删除工作区的文件</strong>。</p></li><li><p><code>git rm &lt;file&gt;</code> 只能在git仓库里使用，是用来删除**[工作区+暂存区]<strong>的文件用的，如果此文件被修改过则要加入参数 <code>-f</code> 来删除</strong>[工作区+暂存区]**的文件。</p></li><li><p><code>git rm --cached &lt;file&gt;</code> 如果想<strong>仅删除暂存区</strong>里的文件可以用此命令，它不会删除工作区中的文件。</p></li></ol><p><strong>建议：</strong></p><ol><li><p>用 <code>rm</code> 或者鼠标点击的方式删除文件。</p></li><li><p>用命令 <code>git rm &lt;file&gt;</code> 确认删除文件。</p></li><li><p>用命令 <code>git restore &lt;file&gt;</code> 返回被删除的文件（无论文件修改与否，返回的都是未修改前的文件）。</p></li></ol></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git rm [options] &lt;file&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="关联远程库-GitHub"><a href="#关联远程库-GitHub" class="headerlink" title="关联远程库 GitHub"></a>关联远程库 GitHub</h3><blockquote><p>在新建空白 GitHub 仓库时，也会生成可以关联 git 的命令。</p><p><strong>repo-name：</strong>克隆仓库名。</p><p><strong>user-name：</strong>克隆仓库的用户名。</p><p><strong>server-name：</strong>基本上是 github.com。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote add origin git@server-name:path/repo-name.git；</span></span><br></pre></td></tr></table></figure><br><h3 id="推送到-GitHub"><a href="#推送到-GitHub" class="headerlink" title="推送到 GitHub"></a>推送到 GitHub</h3><blockquote><p>把本地库的内容推送到远程，用 <code>git push</code> 命令，实际上是把当前分支 <strong>master</strong> 推送到远程。</p><p>由于远程库是空的，我们第一次推送 <strong>master</strong> 分支时，加上了 <code>-u</code> 参数，Git 不但会把本地的 <strong>master</strong> 分支内容推送的远程新的 <strong>master</strong> 分支，还会把本地的 <strong>master</strong> 分支和远程的 <strong>master</strong> 分支关联起来，在以后的推送或者拉取时就可以简化命令。</p><p><strong>origin：</strong>远程库的名称。</p></blockquote><h4 id="本地仓库推送到远程仓库"><a href="#本地仓库推送到远程仓库" class="headerlink" title="本地仓库推送到远程仓库"></a>本地仓库推送到远程仓库</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push &lt;远程仓库名&gt; &lt;本地分支名&gt;:&lt;远程分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="初次推送"><a href="#初次推送" class="headerlink" title="初次推送"></a>初次推送</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push -u origin master</span></span><br></pre></td></tr></table></figure><h4 id="非初次推送"><a href="#非初次推送" class="headerlink" title="非初次推送"></a>非初次推送</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin master</span></span><br></pre></td></tr></table></figure><br><h3 id="远程库操作"><a href="#远程库操作" class="headerlink" title="远程库操作"></a>远程库操作</h3><h4 id="查看远程库"><a href="#查看远程库" class="headerlink" title="查看远程库"></a>查看远程库</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote -v</span></span><br></pre></td></tr></table></figure><h4 id="删除远程库"><a href="#删除远程库" class="headerlink" title="删除远程库"></a>删除远程库</h4><blockquote><p><strong>注意：</strong>此处的“删除”其实是解除了本地和远程的绑定关系，并不是物理上删除了远程库。远程库本身并没有任何改动。要真正删除远程库，需要登录到 GitHub ，在后台页面找到删除按钮再删除。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote rm &lt;远程库名称&gt;</span></span><br></pre></td></tr></table></figure><h4 id="SSH-克隆远程库"><a href="#SSH-克隆远程库" class="headerlink" title="SSH 克隆远程库"></a>SSH 克隆远程库</h4><blockquote><p><strong>repo-name：</strong>克隆仓库名。</p><p><strong>user-name：</strong>克隆仓库的用户名。</p><p><strong>dir：</strong>要克隆到本地的文件目录，默认是”./“。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> git@github.com:user-name/repo-name.git [dir]</span></span><br></pre></td></tr></table></figure><h4 id="HTTPS-克隆远程库"><a href="#HTTPS-克隆远程库" class="headerlink" title="HTTPS 克隆远程库"></a>HTTPS 克隆远程库</h4><blockquote><p><strong>https-link：</strong>克隆仓库的 <code>https</code> 链接。</p><p><strong>dir：</strong>要克隆到本地的文件目录，默认是”./“。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> https-link [dir]</span></span><br></pre></td></tr></table></figure><br><h3 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h3><h4 id="新建分支"><a href="#新建分支" class="headerlink" title="新建分支"></a>新建分支</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch &lt;分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="切换分支"><a href="#切换分支" class="headerlink" title="切换分支"></a>切换分支</h4><blockquote><p><code>-c</code>: 新建分支并切换到该分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git switch [-c] &lt;分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="查看当前分支"><a href="#查看当前分支" class="headerlink" title="查看当前分支"></a>查看当前分支</h4><blockquote><p>当前分支会带”*“号。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch</span></span><br></pre></td></tr></table></figure><h4 id="合并分支"><a href="#合并分支" class="headerlink" title="合并分支"></a>合并分支</h4><blockquote><p><code>git merge</code> 命令用于合并指定分支到当前分支。</p><p><strong>Fast-forward：</strong>合并是“快进模式”，也就是直接把<strong>当前分支</strong>指向<strong>要合并分支</strong>的当前提交，所以合并速度非常快。但这种模式下，删除分支后，会丢掉分支信息。</p><p><code>--no-ff</code>：表示禁用 Fast forward，从分支历史上就可以看出分支信息。</p><p><code>-m</code>：合并要创建一个新的commit。</p><p><strong>分支策略：</strong></p><p>在实际开发中，我们应该按照几个基本原则进行分支管理：</p><p>首先， <code>master</code> 分支应该是非常稳定的，也就是仅用来发布新版本，平时不能在上面干活；</p><p>那在哪干活呢？干活都在 <code>dev</code> 分支上，也就是说， <code>dev</code> 分支是不稳定的，到某个时候，比如1.0版本发布时，再把 <code>dev</code> 分支合并到 <code>master</code> 上，在 <code>master</code> 分支发布1.0版本；</p><p>你和你的小伙伴们每个人都在 <code>dev</code> 分支上干活，每个人都有自己的分支，时不时地往 <code>dev</code> 分支上合并就可以了。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git merge [options] &lt;要合并到当前分支的分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="删除分支"><a href="#删除分支" class="headerlink" title="删除分支"></a>删除分支</h4><blockquote><p><code>-d</code>：删除分支。</p><p><code>-D</code>：强制删除，用于没有合并的分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch -d/-D &lt;分支名&gt;</span> </span><br></pre></td></tr></table></figure><h4 id="BUG-分支"><a href="#BUG-分支" class="headerlink" title="BUG 分支"></a>BUG 分支</h4><ul><li>封存当前工作现场（封存已跟踪文件）。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash</span></span><br></pre></td></tr></table></figure><ul><li>查看封存的工作现场。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash list</span></span><br></pre></td></tr></table></figure><ul><li><p>恢复封存的工作现场</p><ul><li>用 <code>git stash apply</code> 恢复，但是恢复后，stash内容并不删除，你需要用 <code>git stash drop</code> 来删除。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash apply &lt;恢复编号&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash drop &lt;恢复编号&gt;</span></span><br></pre></td></tr></table></figure><ul><li>用 <code>git stash pop</code> ，恢复的同时把stash内容也删了。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash pop</span></span><br></pre></td></tr></table></figure></li><li><p>复制已有 bug 分支（如果是未提交的工作现场，要先封存或提交，才能复制已有 bug 分支）。</p></li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git cherry-pick &lt;bug 版本号&gt;</span></span><br></pre></td></tr></table></figure><h4 id="多人协作"><a href="#多人协作" class="headerlink" title="多人协作"></a>多人协作</h4><p>因此，多人协作的工作模式通常是这样：</p><ol><li>首先，可以试图用 <code>git push origin &lt;branch-name&gt;</code> 推送自己的修改；</li><li>如果推送失败，则因为远程分支比你的本地更新，需要先用 <code>git pull</code> 试图合并；</li><li>如果合并有冲突，则解决冲突，并在本地提交；</li><li>没有冲突或者解决掉冲突后要 <code>add</code> 和 <code>commit</code> 后，再用 <code>git push origin &lt;branch-name&gt;</code> 推送就能成功！</li></ol><p>如果 <code>git pull</code> 提示 <code>no tracking information</code> ，则说明本地分支和远程分支的链接关系没有创建，用命令 <code>git branch --set-upstream-to &lt;branch-name&gt; origin/&lt;branch-name&gt;</code> 。</p><p>这就是多人协作的工作模式，一旦熟悉了，就非常简单。</p><h4 id="整理分支线"><a href="#整理分支线" class="headerlink" title="整理分支线"></a>整理分支线</h4><blockquote><p><strong>特点：</strong>把分叉的提交历史“整理”成一条直线，看上去更直观。缺点是本地的分叉提交已经被修改过了。</p><p><strong>使用：</strong>本地分支过时与远程分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git rebase</span></span><br></pre></td></tr></table></figure><br><h3 id="标签管理"><a href="#标签管理" class="headerlink" title="标签管理"></a>标签管理</h3><h4 id="创建标签"><a href="#创建标签" class="headerlink" title="创建标签"></a>创建标签</h4><blockquote><ul><li>指定分支打标签，首先要切换到指定分支。</li><li><strong>commit id：</strong>指定版本号创建标签。</li><li><strong>options：</strong>用 <code>-a</code> 指定标签名， <code>-m</code> 指定说明文字。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag [options] &lt;标签名&gt; [commit id]</span></span><br></pre></td></tr></table></figure><h4 id="查看标签"><a href="#查看标签" class="headerlink" title="查看标签"></a>查看标签</h4><ul><li>查看所有标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag</span></span><br></pre></td></tr></table></figure><ul><li>查看标签信息</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git show &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="删除标签"><a href="#删除标签" class="headerlink" title="删除标签"></a>删除标签</h4><ul><li>删除标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag -d &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>删除远程标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin :refs/tags/&lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="推送标签"><a href="#推送标签" class="headerlink" title="推送标签"></a>推送标签</h4><ul><li>推送指定标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>推送全部标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin --tags</span></span><br></pre></td></tr></table></figure><br><h3 id="忽略特色文件"><a href="#忽略特色文件" class="headerlink" title="忽略特色文件"></a>忽略特色文件</h3><p>在Git工作区的根目录下创建一个特殊的 <code>.gitignore</code> 文件，然后把要忽略的文件名填进去，Git就会自动忽略这些文件。</p><p>不需要从头写 <code>.gitignore</code> 文件，GitHub 已经为我们准备了各种配置文件，只需要组合一下就可以使用了。所有配置文件可以直接在线浏览：<a href="https://github.com/github/gitignore%E3%80%82">https://github.com/github/gitignore。</a></p><p>忽略文件的原则是：</p><ol><li>忽略操作系统自动生成的文件，比如缩略图等；</li><li>忽略编译生成的中间文件、可执行文件等，也就是如果一个文件是通过另一个文件自动生成的，那自动生成的文件就没必要放进版本库，比如 Java 编译产生的 <code>.class</code> 文件；</li><li>忽略你自己的带有敏感信息的配置文件，比如存放口令的配置文件。</li></ol><p>如果你想添加被忽略的文件，可以用 <code>-f</code> 强制添加到 Git；或者你发现，可能是 <code>.gitignore</code> 写得有问题，需要找出来到底哪个规则写错了，可以用 <code>git check-ignore</code> 命令检查：</p><p>把指定文件排除在 <code>.gitignore</code> 规则外的写法就是 <code>!</code> + 文件名。</p><br><h3 id="配置别名"><a href="#配置别名" class="headerlink" title="配置别名"></a>配置别名</h3><blockquote><p>配置Git的时候，加上 <code>--global</code> 是针对当前用户起作用的，如果不加，那只针对当前的仓库起作用。</p><p>每个仓库的Git配置文件都放在 <code>.git/config</code> 文件中。</p><p>当前用户的Git配置文件放在用户主目录下的一个隐藏文件<code>.gitconfig</code>中。</p></blockquote><ul><li>配置命令</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global <span class="built_in">alias</span>.&lt;别名&gt; &lt;原名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>配置一个 <code>git last</code> ，让其显示最后一次提交信息</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global alias.last <span class="string">&#x27;log -1&#x27;</span></span></span><br></pre></td></tr></table></figure><ul><li>查看日志配置别名</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --global alias.lg &quot;log --color --graph --pretty=format:&#x27;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#x27; --abbrev-commit&quot;</span><br></pre></td></tr></table></figure><br><h3 id="Git-技巧"><a href="#Git-技巧" class="headerlink" title="Git 技巧"></a>Git 技巧</h3><ul><li><p>使用命令 <code>git add &lt;file&gt;</code> ，注意，可反复多次使用，添加多个文件；</p></li><li><p>使用命令 <code>git commit -m &lt;message&gt;</code> ，完成提交文件到本地仓库。</p></li></ul><ul><li><p>要随时掌握工作区的状态，使用 <code>git status</code> 命令。</p></li><li><p>如果 <code>git status</code> 告诉你有文件被修改过，用 <code>git diff</code> 可以查看修改内容。</p></li><li><p> <code>HEAD</code> 指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令 <code>git reset --hard commit_id</code>。</p></li><li><p>穿梭前，用 <code>git log</code> 可以查看提交历史，以便确定要回退到哪个版本。</p></li><li><p>要重返未来，用 <code>git reflog</code> 查看命令历史，以便确定要回到未来的哪个版本。</p></li><li><p>每次修改，如果不用<code>git add</code>到暂存区，那就不会加入到<code>commit</code>中。</p></li><li><p>撤销修改操作：</p><ul><li>场景1：当你改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，用命令 <code>git restore &lt;file&gt;</code>。</li><li>场景2：当你不但改乱了工作区某个文件的内容，还添加到了暂存区时，想丢弃修改，分两步，第一步用命令 <code>git restore --staged &lt;file&gt;</code>，就回到了场景1，第二步按场景1操作。</li><li>场景3：已经提交了不合适的修改到版本库时，想要撤销本次提交，回退版本，不过前提是没有推送到远程库。</li></ul></li><li><p>命令 <code>git rm</code> 用于删除一个文件。如果一个文件已经被提交到版本库，那么你永远不用担心误删，但是要小心，你只能恢复文件到最新版本，你会丢失<strong>最近一次提交后你修改的内容</strong>。</p></li><li><p>建议的删除操作：</p><ol><li><p>用 <code>rm</code> 或者鼠标点击的方式删除文件。</p></li><li><p>用命令 <code>git rm &lt;file&gt;</code> 确认删除文件。</p></li><li><p>用命令 <code>git restore &lt;file&gt;</code> 返回被删除的文件（无论文件修改与否，返回的都是未修改前的文件）。</p></li></ol></li><li><p>要关联一个远程库，使用命令 <code>git remote add origin git@server-name:path/repo-name.git</code> ；</p></li><li><p>关联一个远程库时必须给远程库指定一个名字， <code>origin</code> 是默认习惯命名；</p></li><li><p>关联后，使用命令 <code>git push -u origin master</code> 第一次推送master分支的所有内容；</p></li><li><p>此后，每次本地提交后，只要有必要，就可以使用命令 <code>git push origin master</code> 推送最新修改；</p></li><li><p>要克隆一个仓库，首先必须知道仓库的地址，然后使用<code>git clone</code>命令克隆。</p></li><li><p>Git支持多种协议，包括<code>https</code>，但<code>ssh</code>协议速度最快。</p></li><li><p>Git鼓励大量使用分支：</p><ul><li>查看分支：<code>git branch</code></li><li>创建分支：<code>git branch &lt;name&gt;</code></li><li>切换分支：<code>git checkout &lt;name&gt;</code> 或者 <code>git switch &lt;name&gt;</code></li><li>创建+切换分支：<code>git checkout -b &lt;name&gt;</code> 或者 <code>git switch -c &lt;name&gt;</code></li><li>合并某分支到当前分支：<code>git merge &lt;name&gt;</code></li><li>删除分支：<code>git branch -d &lt;name&gt;</code></li><li>当Git无法自动合并分支时，就必须首先解决冲突。解决冲突后，再提交，合并完成。解决冲突就是把Git合并失败的文件手动编辑为我们希望的内容，再提交。用<code>git log --graph</code>命令可以看到分支合并图。</li></ul></li></ul><ul><li>合并分支时，加上<code>--no-ff</code>参数就可以用普通模式合并，合并后的历史有分支，能看出来曾经做过合并，而<code>fast forward</code> 合并就看不出来曾经做过合并。</li><li>bug 分支<ul><li>修复 bug 时，我们会通过创建新的 bug 分支进行修复，然后合并，最后删除；</li><li>当手头工作没有完成时，先把工作现场 <code>git stash</code> 一下，然后去修复 bug ，修复后，再 <code>git stash pop</code> ，回到工作现场；</li><li>在 master 分支上修复的 bug ，想要合并到当前 dev 分支，可以用 <code>git cherry-pick &lt;commit&gt;</code> 命令，把 bug 提交的修改“复制”到当前分支，避免重复劳动。</li></ul></li><li>开发一个新feature，最好新建一个分支；如果要丢弃一个没有被合并过的分支，可以通过 <code>git branch -D &lt;name&gt;</code> 强行删除。</li><li>多人协作<ul><li>查看远程库信息，使用 <code>git remote -v</code> ；</li><li>本地新建的分支如果不推送到远程，对其他人就是不可见的；</li><li>从本地推送分支，使用 <code>git push origin branch-name</code> ，如果推送失败，先用 <code>git pull</code> 抓取远程的新提交；</li><li>在本地创建和远程分支对应的分支，使用 <code>git switch -c branch-name origin/branch-name</code> ，本地和远程分支的名称最好一致；</li><li>建立本地分支和远程分支的关联，使用 <code>git branch --set-upstream branch-name origin/branch-name</code> ；</li><li>从远程抓取分支，使用 <code>git pull</code> ，如果有冲突，要先处理冲突。</li></ul></li><li><code>rebase</code> 操作可以把本地未 push 的分叉提交历史整理成直线；<code>rebase</code> 的目的是使得我们在查看历史提交的变化时更容易，因为分叉的提交需要三方对比。</li><li>标签管理<ul><li>命令 <code>git tag &lt;tagname&gt;</code> 用于新建一个标签，默认为 <code>HEAD</code> ，也可以指定一个 commit id；</li><li>命令 <code>git tag -a &lt;tagname&gt; -m &quot;blablabla...&quot;</code> 可以指定标签信息；</li><li>命令 <code>git tag</code> 可以查看所有标签；</li><li>命令 <code>git push origin &lt;tagname&gt;</code> 可以推送一个本地标签；</li><li>命令 <code>git push origin --tags</code> 可以推送全部未推送过的本地标签；</li><li>命令 <code>git tag -d &lt;tagname&gt;</code> 可以删除一个本地标签；</li><li>命令 <code>git push origin :refs/tags/&lt;tagname&gt;</code> 可以删除一个远程标签。</li></ul></li><li>在GitHub上，可以任意Fork开源仓库；自己拥有Fork后的仓库的读写权限；可以推送pull request给官方仓库来贡献代码。</li><li>忽略某些文件时，需要编写 <code>.gitignore</code> ；<code>.gitignore</code> 文件本身要放到版本库里，并且可以对 <code>.gitignore</code> 做版本管理！</li></ul><br><br><p><strong>对本文中的不解或本文中存在的错误，欢迎在下方评论区指出！</strong></p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Git </tag>
            
            <tag> 笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo yilia 图片无法显示问题</title>
      <link href="/archives/fbe14a8f.html"/>
      <url>/archives/fbe14a8f.html</url>
      
        <content type="html"><![CDATA[<h3 id="问题说明"><a href="#问题说明" class="headerlink" title="问题说明"></a>问题说明</h3><blockquote><p>关于 Hexo yilia 的图片无法正常显示的问题，从网上总结出三种解决方案：</p><ul><li>第一种方案将图片放置在 blog/source/image/（博客根目录）下，会导致图片在文章展开时无法显示。（无法解决问题）</li><li>第二种方案是通过下载 hexo-asset-image 来解决问题，图片依旧无法显示。（无法解决问题）</li><li>第三种方案是通过下载 <a href="https://github.com/CodeFalling/hexo-asset-image">https://github.com/CodeFalling/hexo-asset-image</a> 来解决问题，图片可以正常显示。（可以解决问题）</li></ul></blockquote><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><ul><li>将博客根目录下的 _config.yml 中的 <code>post _asset_folder: false</code> 改为：</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">post_asset_folder:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><ul><li>下载 hexo-asset-image</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">npm install https://github.com/CodeFalling/hexo-asset-image</span><br><span class="line"><span class="meta">#</span><span class="bash"> 或者</span></span><br><span class="line">cnpm install https://github.com/CodeFalling/hexo-asset-image</span><br></pre></td></tr></table></figure><ul><li>新建文章</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage1.png" alt="新建文章"></p><ul><li>会出现和文章同名的文件夹，把图片素材放到此文件夹</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage2.png" alt="同名文件夹"></p><ul><li>最后在 .md 文件中插入图片即可正常显示</li></ul><blockquote><p><strong>注意：</strong>插入的图片路径要用相对路径</p></blockquote><ul><li>最后再清理、部署、启动服务器即可</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure><br><h3 id="其他问题"><a href="#其他问题" class="headerlink" title="其他问题"></a>其他问题</h3><ol><li>设置路径时，应当用 “./hexoimage/test.jpg” 而不是 “.\hexoimage\test.jpg”。虽然在 Typora 上图片都能正常显示，但是部署到服务器会导致图片名称乱码，因而无法正常显示。</li><li>尽量不要使用 “hexoimage/test.jpg” 或 “/hexoimage/test.jpg” 虽然在 Typora 上都能正常显示，但是部署到服务器可能会出现问题。</li></ol><p><strong>例如：</strong></p><ul><li>下图中的 “.%5Chexoimage%5Ctest.jpg” 就是乱码的 “test.jpg”</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage3.png" alt="乱码路径"></p><br><br><p><strong>最后：希望本文能够帮助你解决问题，文章中有不懂或不正确的地方，请在下方评论区说明！</strong></p>]]></content>
      
      
      <categories>
          
          <category> 疑难杂症 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Hexo </tag>
            
            <tag> yilia </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>欢迎来到我的博客</title>
      <link href="/archives/b250e9bb.html"/>
      <url>/archives/b250e9bb.html</url>
      
        <content type="html"><![CDATA[<p><strong>欢迎来到我的个人博客</strong></p><p><font color="#555555">我将在这里记录：</font></p><ul><li><font color="#555555">我的学习笔记</font></li><li><font color="#555555">我的学习项目经验</font></li><li><font color="#555555">我学习过程中遇到的问题</font></li></ul><p><font color="#555555">希望在提高自己的同时，也能对你有所帮助。</font></p><br><p><font color="#555555">欢迎大家留言，指正我的错误</font></p><p><font color="#555555">或者通过邮箱和QQ互相交流学习经验</font></p>]]></content>
      
      
      <categories>
          
          <category> 日常分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 关于我 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
