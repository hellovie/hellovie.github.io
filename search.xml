<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>bitOperation</title>
      <link href="/archives/16fe89c7.html"/>
      <url>/archives/16fe89c7.html</url>
      
        <content type="html"><![CDATA[<h3 id="常用位运算技巧"><a href="#常用位运算技巧" class="headerlink" title="常用位运算技巧"></a>常用位运算技巧</h3><blockquote><p><strong>位运算符：</strong></p><ol><li>&amp; 与：要求所有表达式的判断结果都是 1 才为 1；</li><li>| 或：要求所有表达式的判断结果都是 0 才为 0；</li><li>^ 异或：如果a、b两个值不相同，则异或结果为 1。如果a、b两个值相同，异或结果为 0；</li><li>! 非：取反。</li></ol></blockquote><h4 id="1-n-amp-n-1-的妙用"><a href="#1-n-amp-n-1-的妙用" class="headerlink" title="1. n &amp; (n - 1) 的妙用"></a>1. n &amp; (n - 1) 的妙用</h4><blockquote><p><strong>描述：</strong>将 n 的二进制表示中的最低位为 1 的改为 0 。</p><p><strong>例子：</strong> n = (1010)<sub>2</sub>、(n - 1) = (1001)<sub>2</sub>，那么 n &amp; ( n - 1) = (1000)<sub>2</sub></p></blockquote><h4 id="2-获取二进制的最高位和最低位"><a href="#2-获取二进制的最高位和最低位" class="headerlink" title="2. 获取二进制的最高位和最低位"></a>2. 获取二进制的最高位和最低位</h4><blockquote><ol><li>获取高位 n &amp; (1 &lt;&lt; 31)</li><li>获取低位 n &amp;1</li></ol></blockquote><br><h3 id="例题"><a href="#例题" class="headerlink" title="例题"></a>例题</h3><h4 id="题目要求"><a href="#题目要求" class="headerlink" title="题目要求"></a>题目要求</h4><p><strong>664 · 数 1：</strong><a href="https://www.lintcode.com/problem/664/">https://www.lintcode.com/problem/664/</a></p><p><strong>描述：</strong>给出一个 <strong>非负</strong> 整数 num，对所有满足 <code>0 ≤ i ≤ num</code> 条件的数字 i 均需要计算其二进制表示中数字 1 的个数并以数组的形式返回。</p><p><strong>样例：</strong></p><p><strong>样例1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">输入： 5</span><br><span class="line">输出： [0,1,1,2,1,2]</span><br><span class="line">解释：</span><br><span class="line">0~5的二进制表示分别是：</span><br><span class="line">000</span><br><span class="line">001</span><br><span class="line">010</span><br><span class="line">011</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">每个数字中1的个数为： 0,1,1,2,1,2</span><br></pre></td></tr></table></figure><p><strong>样例2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入： 3</span><br><span class="line">输出： [0,1,1,2]</span><br></pre></td></tr></table></figure><p><strong>挑战</strong></p><ol><li>时间复杂度为 O(n * sizeof(integer)) 的解法很容易想到, 尝试挑战线性的时间复杂度 O(n) (只遍历一遍)。</li><li>空间复杂度应为 O(n)。</li><li>你能完成这项挑战吗? 不借助任何内嵌的函数, 比如 C++ 中的 <code>__builtin_popcount</code> 亦或是任何其他语言中的方法。</li></ol><h4 id="题目分析"><a href="#题目分析" class="headerlink" title="题目分析"></a>题目分析</h4><ol><li>本题是动态规划和位运算的结合；</li><li>例如 n = (10)<sub>10</sub> = (1010)<sub>2</sub>、，那么 n 中有两个 1；</li><li>运用位运算去掉 n 的最后一个 1；</li><li>即 n &amp; ( n - 1) = (1010)<sub>2</sub> &amp; (1001)<sub>2</sub> = (8)<sub>10</sub> = (1000)<sub>2</sub>；</li><li>用 dp[i] 表示 i 中有几个 1，那么由上可得 dp[10] = dp[8] + 1；</li><li>即状态转移方程为 dp[i] = dp[i &amp; (i - 1)] + 1。</li></ol><h4 id="代码展示"><a href="#代码展示" class="headerlink" title="代码展示"></a>代码展示</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> num: a non negative integer number</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span>: an array represent the number of 1&#x27;s in their binary</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] countBits(<span class="keyword">int</span> num) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// write your code here        </span></span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[num + <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">if</span> (num == <span class="number">0</span>) <span class="keyword">return</span> dp;</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; dp.length; i++) </span><br><span class="line">        &#123;</span><br><span class="line">            dp[i] = dp[i &amp; (i - <span class="number">1</span>)] + <span class="number">1</span>;          </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br><br><p><strong>最后：希望本文能够帮助你解决问题，文章中有不懂或不正确的地方，请在下方评论区说明！</strong></p>]]></content>
      
      
      <categories>
          
          <category> 算法笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 位运算 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>动态规划</title>
      <link href="/archives/a80d0031.html"/>
      <url>/archives/a80d0031.html</url>
      
        <content type="html"><![CDATA[<h3 id="动态规划解题技巧总结"><a href="#动态规划解题技巧总结" class="headerlink" title="动态规划解题技巧总结"></a>动态规划解题技巧总结</h3><blockquote><p>动态规划是把原问题分解为相对简单的子问题的方式求解复杂问题的方法。在用动态规划解题时，应该注意先解题再优化，要把解题放在第一位，这里的解题不做优化。</p></blockquote><h4 id="解题步骤一：确定状态"><a href="#解题步骤一：确定状态" class="headerlink" title="解题步骤一：确定状态"></a>解题步骤一：确定状态</h4><p>确定状态需要三个步骤：</p><ol><li>最后一步：研究最优策略的最后一步，一般是列举最后一步的所有可能进行分析。</li><li>子问题：通过最后一步与倒数第二步或第三步直接的关系发现规律，化为子问题。</li><li>状态数组：根据子问题来创建状态数组。（一般子问题有几个变量就是几维数组）</li></ol><p>我们最终目的是要<strong>确定状态数组的每个元素表示什么</strong>。（状态数组通常是 <code>dp[i][j]</code>）</p><h4 id="解题步骤二：设计状态转移方程"><a href="#解题步骤二：设计状态转移方程" class="headerlink" title="解题步骤二：设计状态转移方程"></a>解题步骤二：设计状态转移方程</h4><blockquote><p>状态转移方程，是动态规划中本阶段的状态往往是上一阶段状态和上一阶段决策的结果。</p></blockquote><p>换言之就是<strong>设计出一个递推公式</strong>。</p><h4 id="解题步骤三：初始条件和边界情况"><a href="#解题步骤三：初始条件和边界情况" class="headerlink" title="解题步骤三：初始条件和边界情况"></a>解题步骤三：初始条件和边界情况</h4><p>初始条件：用状态转移方程无法计算出来，但我们又需要它的定义。即<strong>给出递推公式的基本条件</strong>。</p><p>边界情况：根据题目和状态转移方程判断边界情况，<strong>剔除出界情况</strong>。</p><h4 id="解题步骤四：计算顺序"><a href="#解题步骤四：计算顺序" class="headerlink" title="解题步骤四：计算顺序"></a>解题步骤四：计算顺序</h4><p>状态转移方程是动态规划中本阶段的状态往往是上一阶段状态和上一阶段决策的结果，所以计算顺序要利用之前得出的结果。一般是从小到大，从上到下，从左到右。</p><br><h3 id="例题-1：蓝桥杯印章"><a href="#例题-1：蓝桥杯印章" class="headerlink" title="例题 1：蓝桥杯印章"></a>例题 1：蓝桥杯印章</h3><h4 id="题目要求"><a href="#题目要求" class="headerlink" title="题目要求"></a>题目要求</h4><p><strong>时间限制：</strong>1.0s  </p><p><strong>内存限制：</strong>256.0MB</p><p><strong>问题描述：</strong>共有 n 种图案的印章，每种图案的出现概率相同。小A买了 m 张印章，求小A集齐 n 种印章的概率。</p><p><strong>输入格式：</strong>一行两个正整数 n 和 m。</p><p><strong>输出格式：</strong>一个实数P表示答案，保留4位小数。</p><p><strong>样例输入：</strong>2 3</p><p><strong>样例输出：</strong>0.7500</p><p><strong>数据规模和约定：</strong>1≤n，m≤20</p><p><strong>题目链接</strong>：<a href="http://lx.lanqiao.cn/problem.page?gpid=T3002">http://lx.lanqiao.cn/problem.page?gpid=T3002</a></p><h4 id="题目分析"><a href="#题目分析" class="headerlink" title="题目分析"></a>题目分析</h4><p>我们现在有 n 种图案，买了 m 张，集齐概率为 P<sub>m</sub>，我们买的每张印章概率都为 1/n，我们先假设 m &gt;= n ，如果 m &lt; n 的话是不可能集齐 n 种的。如果要暴力解题，就需要遍历所有可能再求概率，但是我们发现已经买 n - 1张集齐了 k 种的概率可以被买 n 张集齐了 k 种的情况或者买 n 张集齐了 k + 1 种的情况使用，那么我们就可以用一个数组来记录之前求过的概率，减少重复运算，就可以用动态规划解题。</p><ul><li><strong>确定状态</strong></li></ul><ol><li><p>最后一步：买第 m 张的印章，这时第 m 张可能是不重复的印章也可能是重复的印章。</p></li><li><p>子问题：</p><ul><li>如果第 m 张是不重复的话，P<sub>m</sub> = P<sub>m - 1</sub> * (1/n)；因为第 m - 1 张已经集齐了 n - 1 种，所以第 m 张要抽到唯一 1 张没抽过的；</li><li>如果第 m 张是重复的话，P<sub>m</sub> = P<sub>m - 1</sub> * 1，因为第 m - 1 张已经集齐了 n 种，所以第 m 张不管是抽到哪种都不影响；</li><li>那么现在问题就变成求 P<sub>m -  1</sub>，即买 m - 1 张集齐 n 种或买 m - 1 张集齐 n - 1种；</li><li>我们发现再以此类推，存在两个变量，那么得出子问题就是求买 i 张，集齐 j 张的概率。</li></ul></li><li><p>状态数组：用 dp[i][j] 表示买了 i 张，集齐 j 种的概率。</p></li></ol><ul><li><strong>设计状态转移方程</strong></li></ul><blockquote><p><strong>(n - j + 1) / n：</strong>表示在 n 种印章中，抽出剩下的 n - j + 1 种印章的概率。</p><p><strong>(j / n)：</strong>表示在 n 种印章中，抽出已抽的 j 种印章的概率。</p></blockquote><ol><li><p>买第 i 张时集齐 j 张的概率（没有重复）：dp[i][j] = dp[i - 1][j - 1] * (n - j + 1) / n。</p></li><li><p>买第 i 张时集齐 j 张的概率（有重复）：dp[i][j] = dp[i - 1][j] * (j / n)。</p></li><li><p><font color=red>dp[i][j] = dp[i - 1][j - 1] * (n - j + 1) / n + dp[i - 1][j] * (j / n)</font>。</p></li></ol><ul><li><strong>初始条件和边界情况</strong></li></ul><ol><li>当 i &lt; j 时，无法买 i 张去集齐 j 张，这时 dp[i][j] = 0；</li><li>当 j = 1，i &gt;= 1，表示买 i 张去集齐 1 张；已知每张印章概率都为 1/n，共有 n 种印章，所以在 n 种印章中抽 i 张，抽中的都是同一种印章的概率为：dp[i][j] = (1/n)<sup>i</sup> * n。</li></ol><ul><li><strong>计算顺序</strong></li></ul><p>从小到大，因为后面的概率需要用到前面得出的结果。</p><h4 id="代码展示"><a href="#代码展示" class="headerlink" title="代码展示"></a>代码展示</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 共有n种图案的印章，每种图案的出现概率相同。小A买了 m 张印章，求小A集齐 n 种印章的概率。</span></span><br><span class="line">        Scanner scan = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        <span class="keyword">int</span> n = scan.nextInt();</span><br><span class="line">        <span class="keyword">int</span> m = scan.nextInt();</span><br><span class="line">        scan.close();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// dp[i][j]表示买 i 张凑齐 j 种印章的概率。</span></span><br><span class="line">        <span class="keyword">double</span>[][] dp = <span class="keyword">new</span> <span class="keyword">double</span>[m + <span class="number">1</span>][n + <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= m; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (i &lt; j) dp[i][j] = <span class="number">0</span>;</span><br><span class="line">                <span class="keyword">if</span> (j == <span class="number">1</span>) dp[i][j] = Math.pow(<span class="number">1.0</span> / n, i - <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">else</span> dp[i][j] = dp[i - <span class="number">1</span>][j] * (j * <span class="number">1.0</span> / n) + </span><br><span class="line">                    dp[i - <span class="number">1</span>][j - <span class="number">1</span>] * ((n - j + <span class="number">1</span>) * <span class="number">1.0</span> / n);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.format(<span class="string">&quot;%.4f&quot;</span>, dp[m][n]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br><h3 id="例题-2：数字三角形"><a href="#例题-2：数字三角形" class="headerlink" title="例题 2：数字三角形"></a>例题 2：数字三角形</h3><h4 id="题目要求-1"><a href="#题目要求-1" class="headerlink" title="题目要求"></a>题目要求</h4><p><strong>时间限制：</strong>1.0s  </p><p><strong>内存限制：</strong>256.0MB</p><p><strong>问题描述：</strong>（图３.１－１）示出了一个数字三角形。 请编一个程序计算从顶至底的某处的一条路径，使该路径所经过的数字的总和最大。</p><ul><li><p>每一步可沿左斜线向下或右斜线向下走；</p></li><li><p>1＜三角形行数≤100；</p></li><li><p>三角形中的数字为整数0，1，…99；　</p><p>　<img src="http://lx.lanqiao.cn/RequireFile.do?fid=9RdAy6Be" alt="img"><br>  　（图３.１－１)</p></li></ul><p><strong>输入格式：</strong>文件中首先读到的是三角形的行数。接下来描述整个三角形</p><p><strong>输出格式：</strong>最大总和（整数）</p><p><strong>样例输入：</strong></p><p>5<br>7<br>3 8<br>8 1 0<br>2 7 4 4<br>4 5 2 6 5</p><p><strong>样例输出：</strong>30</p><p><strong>题目链接</strong>：<a href="http://lx.lanqiao.cn/problem.page?gpid=T312">http://lx.lanqiao.cn/problem.page?gpid=T312</a></p><h4 id="题目分析-1"><a href="#题目分析-1" class="headerlink" title="题目分析"></a>题目分析</h4><p>计算从第 1 层到第 n 层的一条路径，求数字总和最大值。如果用暴力解法的话，我们就需要遍历每一条路径，再求最大值，时间复杂度高。我们发现如果从第 1 层走到第 n 层的第 k 个数或第 k + 1 个数的数字总和都需要用到从第 1 层走到第 n - 1 层的第 k 个数的数字总和，那么我们就可以用一个数组来保存之前的数字总和，减少重复运算，就可以用动态规划解题。</p><ul><li><strong>确定状态</strong></li></ul><ol><li>最后一步：假设用一个 二维数组 M[][] 来表示三角形每个数字的位置，那么最顶层的那个数就是 M[1][1]。假设倒数第二步就是 M[n - 1][j]，那么最后一步就可能是M[n][j] 或 M[n][j + 1]（每一步可沿左斜线向下或右斜线向下走）。</li><li>子问题：<ul><li>那么，从第 n - 1 层的 j 处到第 n 层的数字总和最大值 = M[n - 1][j] + Max{M[n][j], M[n][j + 1]}，以此类推，可以得出第 n - 1 层每个数到第 n 层的数字最大总和；</li><li>那么问题就变成了从第 1 层到第 n - 1 层的一条路径，求数字总和最大值；</li><li>我们又可以得出第 n - 2 层每个数到第 n - 1 层的数字最大总和，第 n - 2 层的对应数到第 n 层的数字最大总和 = 第 n - 2 层对应数 + 第 n - 1 层对应数到第 n 层的数字最大总和（上面得出的），从而得出从第 n - 2 层到第 n 层的数字总和最大值；</li><li>不断递推，我们每次都能得出第 i 层的第 j 个数到第 n 层的数字总和最大值。</li><li>那么子问题就是求第 i 层的第 j 个数到第 n 层的数字总和最大值。</li></ul></li><li>状态数组：那么我们就用 dp[i][j] 表示第 i 层的第 j 个数到第 n 层的数字总和最大值。</li></ol><ul><li><strong>设计状态转移方程</strong></li></ul><blockquote><p><strong>M[i][j]：</strong>三角形每个数字的位置。</p><p><strong>dp[i][j]：</strong>第 i 层的第 j 个数到第 n 层的数字总和最大值。</p></blockquote><p><font color="red">dp[i][j] = Max(dp[i + 1][j], dp[i +1][j]) + M[i][j]</font>。</p><ul><li><strong>初始条件和边界情况</strong></li></ul><ol><li>dp[n][j] = M[n][j]；</li><li>因为第 n 层 dp 已经初始化，所以 n &gt; i &gt;= 1；</li><li>i &gt;= j &gt;= 1。</li></ol><ul><li><strong>计算顺序</strong></li></ul><p>自底向上。</p><h4 id="代码展示-1"><a href="#代码展示-1" class="headerlink" title="代码展示"></a>代码展示</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        Scanner scan = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">        <span class="keyword">int</span> n = scan.nextInt();</span><br><span class="line">        <span class="keyword">int</span> M[][] = <span class="keyword">new</span> <span class="keyword">int</span>[n + <span class="number">1</span>][n + <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= i; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                M[i][j] = scan.nextInt();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        scan.close();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// dp[i][j]表示第 i 层的第 j 个数到第 n 层的数字总和最大值。</span></span><br><span class="line">        <span class="keyword">int</span> dp[][] = <span class="keyword">new</span> <span class="keyword">int</span>[n + <span class="number">1</span>][n + <span class="number">1</span>];</span><br><span class="line">        <span class="comment">// 初始化初始条件</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            dp[n][i] = M[n][i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 第 i 层的第 j 个数到第 n 层的数字总和最大值。</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt;= <span class="number">1</span>; i--)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= i; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                dp[i][j] = Math.max(dp[i + <span class="number">1</span>][j], dp[i + <span class="number">1</span>][j + <span class="number">1</span>]) + M[i][j];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(dp[<span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li><p>首先分析问题中是否存在许多重复计算，能够使用动态规划。</p></li><li><p>其次按照动态规划解题技巧总结：</p><ul><li><p>确定状态：</p><ul><li><p>最后一步；</p></li><li><p>子问题；</p></li><li><p>状态数组。</p></li></ul></li><li><p>设计状态转移方程；</p></li><li><p>初始条件和边界情况；</p></li><li><p>计算顺序。</p></li></ul></li><li><p>最后优化空间。</p></li><li><p>记住动态规划类型题只能通过多做来熟悉。</p></li></ul><br><br><p><strong>最后：希望本文能够帮助你解决问题，文章中有不懂或不正确的地方，请在下方评论区说明！</strong>   </p>]]></content>
      
      
      <categories>
          
          <category> 算法笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 动态规划 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++ const理解</title>
      <link href="/archives/9e115568.html"/>
      <url>/archives/9e115568.html</url>
      
        <content type="html"><![CDATA[<h3 id="const-的作用"><a href="#const-的作用" class="headerlink" title="const 的作用"></a>const 的作用</h3><p>使用类型修饰符 <strong>const</strong> 说明的类型成为常类型，常类型的变量或对象的值是不能被更新的。所以在编程过程中存在保持不变的值时应当用 <strong>const</strong> 限定。</p><br><h3 id="const-的基本使用"><a href="#const-的基本使用" class="headerlink" title="const 的基本使用"></a>const 的基本使用</h3><p><strong>注意：因为 const 对象一旦创建后其值就不能再改变，所以 const 对象必须要初始化。</strong></p><p>初始化 bufsize 的值：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> bufSize = <span class="number">512</span>;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;bufSize = &quot;</span> &lt;&lt; bufSize;</span><br><span class="line"><span class="comment">// 输出：bufSize = 512</span></span><br></pre></td></tr></table></figure><br><h3 id="默认状态下，const-对象仅在文件内有效"><a href="#默认状态下，const-对象仅在文件内有效" class="headerlink" title="默认状态下，const 对象仅在文件内有效"></a>默认状态下，const 对象仅在文件内有效</h3><p><strong>注意：如果想在多个文件之间共享 const 对象，必须在变量的定义之前添加 extern 关键字。</strong></p><p>解决 const 对象仅在文件内有效的方法：<strong>对于 const 变量不管是声明还是定义都添加 extern 关键字</strong>，这样只需定义一次就行。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// file1.cpp</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">const</span> <span class="keyword">int</span> ext = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file2.cpp</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">const</span> <span class="keyword">int</span> ext;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; ext &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出：12</span></span><br></pre></td></tr></table></figure><br><h3 id="const-的引用"><a href="#const-的引用" class="headerlink" title="const 的引用"></a>const 的引用</h3><blockquote><p>“对 const 的引用”简称为“常量引用”。</p></blockquote><p>把引用绑定到 const 对象上，就像绑定到其他对象上一样，称为<strong>对常量的引用</strong>。与普通引用不同，对常量的引用不能被用作修改它所绑定的对象：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> ci = <span class="number">1024</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r1 = ci; <span class="comment">// 正确：引用和它对应的对象都是常量</span></span><br><span class="line">r1 = <span class="number">42</span>; <span class="comment">// 错误：r1 是对常量的引用，r1 是不可修改的左值</span></span><br><span class="line"><span class="keyword">int</span>&amp; r2 = ci; <span class="comment">// 错误：试图让一个非常量引用指向一个常量对象，将 &quot;int&amp;&quot; 类型的引用绑定到 &quot;const int&quot; 类型的初始值设定项时，限定符被丢弃</span></span><br></pre></td></tr></table></figure><p><strong>注意：引用的类型必须与其所引用的对象类型一致。</strong>但是存在两种例外。</p><p>在初始化<strong>常量引用</strong>时<strong>允许用任意表达式作为初始量</strong>，只要该表达式的结果能转换成引用的类型即可。尤其，允许为一个常量引用绑定非常量的对象、字面量、甚至是个一般表达式：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">42</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r1 = i; <span class="comment">// 允许将 const int&amp; 绑定到一个普通 int 对象上</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r2 = <span class="number">42</span>; <span class="comment">// 正确：r2 是一个常量引用</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r3 = r1 * <span class="number">2</span>; <span class="comment">// 正确：r3 是一个常量引用</span></span><br><span class="line"><span class="keyword">int</span>&amp; r4 = r1 * <span class="number">2</span>; <span class="comment">// 错误：r4 是一个普通的非常量引用，非常量引用的初始值必须为左值（可修改的值）</span></span><br></pre></td></tr></table></figure><p><strong>常量引用（对 const 的引用）可能引用一个并非 const 的对象</strong></p><p><strong>常量引用仅对引用可参与的操作做出了限定</strong>，对引用的对象本身是不做限定的。因为对象也可能是个非常量，所以允许通过其他途径改变它的值：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">42</span>;</span><br><span class="line"><span class="keyword">int</span>&amp; r1 = i; <span class="comment">// 引用 ri 绑定对象 i</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r2 = i; <span class="comment">// r2 也绑定对象 i，但是不允许通过 r2 修改 i 的值</span></span><br><span class="line">r1 = <span class="number">0</span>; <span class="comment">// r1 并非常量引用，i 的值修改为 0</span></span><br><span class="line">r2 = <span class="number">0</span>; <span class="comment">// 错误：r2 是一个常量引用</span></span><br></pre></td></tr></table></figure><p><font color="red">为什么常量引用可以绑定一个与其类型不一样的对象呢？</font></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">double</span> i = <span class="number">42.5</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r1 = i; <span class="comment">// 正确：输出 42</span></span><br><span class="line"><span class="keyword">int</span>&amp; r2 = i; <span class="comment">// 错误：无法用 &quot;double&quot; 类型的值初始化 &quot;int&amp;&quot; 类型的引用(非常量限定)</span></span><br></pre></td></tr></table></figure><p>因为要保证 r1 能够绑定一个整型，会先把 i 赋值给一个整型的临时量，再让 r1 绑定这个临时量。由于 r1 绑定的是临时量而非 i，这就让引用失去了它原本的含义，所以 C++ 也把这种行为归为非法。如果是常量引用，这个引用无法被修改，自然就不会破坏引用的意义。</p><p><strong>拓展：</strong></p><ul><li>左值是可寻址的变量；</li><li>右值一般是不可寻址的常量；</li><li>左值一般可以被修改，而右值不能。</li></ul><br><h3 id="指针和-const"><a href="#指针和-const" class="headerlink" title="指针和 const"></a>指针和 const</h3><p><strong>与 const 相关的指针：</strong></p><ol><li><code>const int * p</code> 或者 <code>int const * p</code>：指向 const 对象的指针或者说是指向常量的指针。</li><li><code>int * const p</code>：常量指针、const 指针。</li><li><code>const int * const p</code>：指向常量对象的常量指针。</li></ol><p><strong>从上面我们可以看出，当 <code>*</code> 比 const 离变量名更近时说明它先是一个指针，称为指向常量的指针；当 const 比 <code>*</code> 离变量名更近时说明它先是一个常量，称为常量指针。</strong></p><h4 id="指向常量的指针"><a href="#指向常量的指针" class="headerlink" title="指向常量的指针"></a>指向常量的指针</h4><p><strong>指向常量的指针</strong>不能用于改变其所指对象的值。<strong>要想存放常量对象的地址，只能使用指向常量的指针。</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">double</span> PI = <span class="number">3.14</span>; <span class="comment">// PI 是一个常量，它的值不能改变</span></span><br><span class="line"><span class="keyword">double</span>* ptr = &amp;PI; <span class="comment">// 错误：ptr 是一个普通指针，&quot;const double *&quot; 类型的值不能用于初始化 &quot;double *&quot; 类型的实体</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">double</span>* p = <span class="number">10.0</span>; <span class="comment">// 错误：&quot;double&quot; 类型的值不能用于初始化 &quot;const double *&quot; 类型的实体</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">double</span>* cptr = &amp;PI; <span class="comment">// 正确：cptr 可以指向一个双精度常量</span></span><br><span class="line">*cptr = <span class="number">42</span>; <span class="comment">// 错误：不能给 *cptr 赋值，表达式必须是可修改的左值</span></span><br></pre></td></tr></table></figure><p>和<strong>引用</strong>一样，<strong>指针的类型必须与其所引用的对象类型一致</strong>，但是允许一个<strong>指向常量的指针</strong>指向一个<strong>非常量对象</strong>：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">double</span> PI = <span class="number">3.14</span>; <span class="comment">// PI 是一个常量，它的值不能改变</span></span><br><span class="line"><span class="keyword">double</span> dval = <span class="number">3.14</span>; <span class="comment">// dval 是一个双精度浮点数，它的值可以改变</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">double</span>* cptr = &amp;PI; <span class="comment">// 正确：cptr 可以指向一个双精度常量</span></span><br><span class="line">cptr = &amp;dval; <span class="comment">// 正确：但是不能通过 cptr 改变 dval 的值</span></span><br></pre></td></tr></table></figure><p><strong>注意：虽然不能给 <code>*cptr</code> 赋值，但是我们可以让 <code>cptr</code> 重新指向新的双精度常量。</strong></p><p>和<strong>常量引用</strong>一样，所谓<strong>指向常量的指针</strong>仅仅只是<strong>不允许通过该指针去修改对象的值</strong>，并没有限制用其他途径去修改对象的值。</p><h4 id="const-指针"><a href="#const-指针" class="headerlink" title="const 指针"></a>const 指针</h4><p>指针是对象而引用不是，因此<strong>允许把指针本身定义为常量</strong>。<strong>常量指针</strong>必须<strong>初始化</strong>，而且一旦初始化完成，则它的值（也就是存放在指针中的那个地址）就不能再改变了。把 <code>*</code> 放在 <strong>const</strong> 关键字之前说明指针是一个常量，即不变的是指针本身，而非指针指向的值。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> errNum = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span>* <span class="keyword">const</span> curErr = &amp;errNum; <span class="comment">// curErr 将一直指向 errNum</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">double</span> PI = <span class="number">3.14</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">double</span>* <span class="keyword">const</span> pip = &amp;PI; <span class="comment">// pip 是一个指向常量对象的常量指针</span></span><br></pre></td></tr></table></figure><p><strong>注意：常量指针并不意味着不能用指针修改所指向对象的值。</strong></p><p>如果我们用常量指针去指向一个常量，也会出现错误，因为常量指针是可以修改指向对象的值的：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> num = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span>* <span class="keyword">const</span> p = &amp;num; <span class="comment">// 错误：&quot;const int*&quot; 类型的值不能用于初始化 &quot;int *const&quot; 类型的实体</span></span><br></pre></td></tr></table></figure><p>所以我们可以总结出：</p><ol><li>如果想要用指针指向常量，我们应该用<strong>指向常量的指针</strong>。</li><li>如果只想无法通过指针去修改指向对象的值，我们应该用<strong>常量指针</strong>，前提是指向的对象是一个非常量。</li></ol><br><h3 id="顶层-const-和底层-const"><a href="#顶层-const-和底层-const" class="headerlink" title="顶层 const 和底层 const"></a>顶层 const 和底层 const</h3><p><strong>top-level 顶层 const:</strong> 指针本身是个常量（常量指针）。</p><p><strong>low-level 底层 const:</strong> 指针所指向的对象是个常量（指向常量的指针）。</p><p><strong>引用就是一个常量指针，不能更改绑定的对象地址；若是常量引用（即 <code>const&amp;</code> ），能绑定常量，并且不能用常量引用修改对象的值，但是如果对象不是常量，则可以用其他方式可以修改。</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span>* <span class="keyword">const</span> p1 = &amp;i; <span class="comment">// 不能改变 p1 的值，只是一个顶层 const（常量指针）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> c1 = <span class="number">42</span>; <span class="comment">// 不能改变 c1 的值，只是一个顶层 const（常量）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* p2 = &amp;c1; <span class="comment">// 允许改变 p2 的值，只是一个低层 const（指向常量的指针）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* <span class="keyword">const</span> p3 = p2; <span class="comment">// 靠右的 const 是顶层 const，靠左的是底层 const（指向常量对象的常量指针）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r = c1; <span class="comment">// 用于声明引用的 const 都是底层 const（常量引用）</span></span><br></pre></td></tr></table></figure><p>在拷贝对象时，<strong>顶层 const</strong> 和<strong>底层 const</strong> 的限制有所不同：</p><ol><li><strong>顶层 const</strong> 对拷贝对象时，<strong>无影响</strong>；因为拷贝对象时，并不会修改被拷贝对象的值，所以无论是拷入还是拷出的对象是否为常量都没有什么影响。</li><li><strong>底层 const</strong> 对拷贝对象时，<strong>有影响</strong>；拷入和拷出必须具有相同的<strong>底层 const</strong> 资格，或者两个对象的数据类型必须能转换。一般来说，非常量可以转换成常量，反之不行。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> c1 = <span class="number">42</span>; <span class="comment">// 不能改变 c1 的值，只是一个顶层 const（常量）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* p2 = &amp;c1; <span class="comment">// 允许改变 p2 的值，只是一个低层 const（指向常量的指针）</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* <span class="keyword">const</span> p3 = p2; <span class="comment">// 靠右的 const 是顶层 const，靠左的是底层 const（指向常量对象的常量指针）</span></span><br><span class="line"><span class="comment">// 顶层 const</span></span><br><span class="line">i = c1; <span class="comment">// 正确：拷贝 c1 的值，c1 是一个顶层 const，对此无影响</span></span><br><span class="line">p2 = p3; <span class="comment">// 正确：p2 和 p3 所指向的对象类型相同，p3 顶层 const 的部分不受影响</span></span><br><span class="line"><span class="comment">// 底层 const</span></span><br><span class="line"><span class="keyword">int</span>* p = p3; <span class="comment">// 错误：p3 包含底层 const 的定义，而 p 没有</span></span><br><span class="line">p2 = p3; <span class="comment">// 正确：p2 和 p3 都是底层 const</span></span><br><span class="line">p2 = &amp;i; <span class="comment">// 正确：int* 能转换成 const int*</span></span><br><span class="line"><span class="keyword">int</span>&amp; r = c1; <span class="comment">// 错误：普通的 int&amp; 不能绑定到 int 常量上</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>&amp; r2 = i; <span class="comment">// 正确：const int&amp; 可以绑定到一个普通的 int 上</span></span><br></pre></td></tr></table></figure><p>总之就是要考虑在拷贝后会不会影响原来对象的原有限制。</p><br><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol><li>在编程过程中存在保持不变的值时应当用 <strong>const</strong> 限定。</li><li>因为 <strong>const</strong> 对象一旦创建后其值就<strong>不能再改变</strong>，所以 <strong>const</strong> 对象必须要<strong>初始化</strong>。</li><li>如果想在多个文件之间共享 <strong>const</strong> 对象，必须在变量的定义之前添加 <strong>extern</strong> 关键字。</li><li>用<strong>常量引用</strong>绑定对象，无法通过<strong>常量引用</strong>修改对象的值；但如果对象是个非常量的话，可以通过其他途径修改对象的值。</li><li>在初始化<strong>常量引用</strong>时允许用<strong>任意表达式</strong>作为初始量，只要该表达式的结果能转换成引用的类型即可。</li><li><strong>const 和指针：</strong><ul><li><code>const char * p</code> 或者 <code>char const * p</code>：指向常量的指针，不允许通过该指针去修改对象的值。</li><li><code>char * const p</code>：常量指针，初始化后存放在指针中的那个地址不能再改变。</li><li>要想存放常量对象的地址，只能使用<strong>指向常量的指针</strong>，允许一个<strong>指向常量的指针</strong>指向一个<strong>非常量对象</strong>。</li><li>虽然不能给<strong>指向常量的指针</strong>赋值，但是我们可以让<strong>指向常量的指针</strong>重新指向新的双精度常量。</li><li><strong>常量指针</strong>必须<strong>初始化</strong>，而且一旦初始化完成，则它的值（也就是存放在指针中的那个地址）就不能再改变了。</li><li><strong>常量指针</strong>并不意味着不能用指针修改所指向对象的值。</li><li>如果想要用指针指向常量，我们应该用<strong>指向常量的指针</strong>。</li><li>如果只想无法通过指针去修改指向对象的值，我们应该用<strong>常量指针</strong>，前提是指向的对象是一个非常量。</li></ul></li><li><strong>top-level 顶层 const：</strong>指针本身是个常量（常量指针）。</li><li><strong>low-level 底层 const：</strong> 指针所指向的对象是个常量（指向常量的指针）。</li><li>在拷贝对象时，<strong>顶层 const 无影响</strong>，而<strong>底层 const 有影响</strong>；拷入和拷出必须具有相同的<strong>底层 const</strong> 资格，或者两个对象的数据类型必须能转换。一般来说，非常量可以转换成常量，反之不行。</li></ol><br><br><p><strong>最后：希望本文能够帮助你解决问题，文章中有不懂或不正确的地方，请在下方评论区说明！</strong></p><p><strong>参考文献：</strong></p><ul><li>C++  Primer中文版  第5版 [（美）李普曼，（美）拉乔伊，（美）默著][电子工业出版社][2013.08][838页]</li></ul>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++ </tag>
            
            <tag> Const </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Git 学习笔记</title>
      <link href="/archives/3c3cdb74.html"/>
      <url>/archives/3c3cdb74.html</url>
      
        <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>最近用 GitHub 搭建了个人博客，顺便再回顾一下之前了解过但是没熟练掌握的 Git ，本篇主要是记录关于 Git 的一些命令和技巧，学习途径——<strong>廖雪峰Git教程</strong>，详细教程可以阅读<a href="https://www.liaoxuefeng.com/wiki/896043488029600" title="廖雪峰Git教程">廖雪峰Git教程</a>。</p><p><strong>git 新更改：</strong> <code>git checkout</code> 可替换命令 <code>git switch</code> 和 <code>git restore</code> ，这里的笔记以新更改为主。</p><br><h3 id="基本配置"><a href="#基本配置" class="headerlink" title="基本配置"></a>基本配置</h3><blockquote><p>–global：设置全局变量，局部变量则无需 <code>--global</code> 参数。</p></blockquote><h4 id="设置用户信息"><a href="#设置用户信息" class="headerlink" title="设置用户信息"></a>设置用户信息</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.name <span class="string">&quot;用户名&quot;</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email <span class="string">&quot;用户邮箱&quot;</span></span></span><br></pre></td></tr></table></figure><h4 id="查看配置信息"><a href="#查看配置信息" class="headerlink" title="查看配置信息"></a>查看配置信息</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.name</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email</span></span><br></pre></td></tr></table></figure><br><h3 id="Git-基础操作"><a href="#Git-基础操作" class="headerlink" title="Git 基础操作"></a>Git 基础操作</h3><h4 id="初始化-Git-仓库"><a href="#初始化-Git-仓库" class="headerlink" title="初始化 Git 仓库"></a>初始化 Git 仓库</h4><blockquote><p><strong>工作区(workspace)：</strong></p><ul><li><strong>未跟踪(untracked)：</strong>新创建文件。</li><li><strong>未暂存(unstaged)：</strong>修改已有文件。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git init</span></span><br></pre></td></tr></table></figure><h4 id="把文件添加到仓库"><a href="#把文件添加到仓库" class="headerlink" title="把文件添加到仓库"></a>把文件添加到仓库</h4><blockquote><p><strong>工作区(workspace) –&gt; 暂存区(index)</strong></p><p><strong>已暂存(staged)：</strong>提交到仓库之前的缓存区。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git add &lt;file&gt;</span></span><br></pre></td></tr></table></figure><h4 id="把文件提交到仓库"><a href="#把文件提交到仓库" class="headerlink" title="把文件提交到仓库"></a>把文件提交到仓库</h4><blockquote><p><strong>暂存区(index) –&gt; 本地仓库(repository)</strong></p><p><strong>说明：</strong>如果把文件提交到仓库后，工作区(workspace)无操作，则暂存区(index)是干净的。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git commit -m <span class="string">&quot;本次提交的说明&quot;</span></span></span><br></pre></td></tr></table></figure><h4 id="查看文件状态"><a href="#查看文件状态" class="headerlink" title="查看文件状态"></a>查看文件状态</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git status</span></span><br></pre></td></tr></table></figure><h4 id="查看文件不同"><a href="#查看文件不同" class="headerlink" title="查看文件不同"></a>查看文件不同</h4><blockquote><ol><li><code>git diff</code>：是查看 working tree 与 index 的差别的。</li><li><code>git diff --cached</code>：是查看 index 与 repository 的差别的。</li><li><code>git diff HEAD</code>：是查看 working tree 和 repository 的差别的。其中：HEAD 代表的是最近的一次 commit 的信息。</li></ol></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git diff &lt;file&gt;</span></span><br></pre></td></tr></table></figure><h4 id="显示提交日志"><a href="#显示提交日志" class="headerlink" title="显示提交日志"></a>显示提交日志</h4><blockquote><p><strong>options</strong>：</p><ul><li><code>--all</code>：显示所有分支。</li><li><code>--pretty=oneline</code>：将提交信息显示为一行。</li><li><code>--abbrev-commit</code>：使得输出的commit更简短。</li><li><code>--graph</code>：以图的形式显示。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span> [options]</span></span><br></pre></td></tr></table></figure><h4 id="查看历史命令"><a href="#查看历史命令" class="headerlink" title="查看历史命令"></a>查看历史命令</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reflog</span></span><br></pre></td></tr></table></figure><h4 id="撤销修改"><a href="#撤销修改" class="headerlink" title="撤销修改"></a>撤销修改</h4><blockquote><p><strong>无参：</strong>撤销工作区(workspace)的修改。</p><p><code>--staged</code>：撤销上传到暂存区(index)。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git restore [--staged] &lt;file&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="回退版本"><a href="#回退版本" class="headerlink" title="回退版本"></a>回退版本</h3><h4 id="回退上个版本"><a href="#回退上个版本" class="headerlink" title="回退上个版本"></a>回退上个版本</h4><blockquote><p>回退多个版本可以把 <code>^</code> 换成 <code>~&lt;回退版本数&gt;</code>。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard HEAD^</span></span><br></pre></td></tr></table></figure><h4 id="回退到指定版本"><a href="#回退到指定版本" class="headerlink" title="回退到指定版本"></a>回退到指定版本</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard &lt;commit id&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="删除文件"><a href="#删除文件" class="headerlink" title="删除文件"></a>删除文件</h3><blockquote><p><strong>注意：从来没有被添加到版本库就被删除的文件，是无法恢复的！</strong></p><p><strong>文件修改后，与版本库不同时，再删除，此时options：</strong></p><ul><li><p><code>--cached</code>：仅删除暂存区里的文件。此时 commit ，会出现工作区存在修改后的删除文件，而版本库此文件被删除。</p></li><li><p><code>-f </code>：强制删除[工作区+暂存区]。此时 commit ，会出现工作区和版本库此文件都删除。</p></li></ul><p><strong>理解：</strong></p><ol><li><p><code>rm &lt;file&gt;</code> 可以在任何文件夹都可以用，包括不是git仓库的地方，是用来删除文件。如果用在git仓库里，则可以视为<strong>删除工作区的文件</strong>。</p></li><li><p><code>git rm &lt;file&gt;</code> 只能在git仓库里使用，是用来删除<strong>（工作区+暂存区）</strong>的文件用的，如果此文件被修改过则要加入参数 <code>-f</code> 来删除<strong>（工作区+暂存区）</strong>的文件。</p></li><li><p><code>git rm --cached &lt;file&gt;</code> 如果想<strong>仅删除暂存区</strong>里的文件可以用此命令，它不会删除工作区中的文件。</p></li></ol><p><strong>建议：</strong></p><ol><li><p>用 <code>rm</code> 或者鼠标点击的方式删除文件。</p></li><li><p>用命令 <code>git rm &lt;file&gt;</code> 确认删除文件。</p></li><li><p>用命令 <code>git restore &lt;file&gt;</code> 返回被删除的文件（无论文件修改与否，返回的都是未修改前的文件）。</p></li></ol></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git rm [options] &lt;file&gt;</span></span><br></pre></td></tr></table></figure><br><h3 id="关联远程库-GitHub"><a href="#关联远程库-GitHub" class="headerlink" title="关联远程库 GitHub"></a>关联远程库 GitHub</h3><blockquote><p>在新建空白 GitHub 仓库时，也会生成可以关联 git 的命令。</p><p><strong>repo-name：</strong>克隆仓库名。</p><p><strong>user-name：</strong>克隆仓库的用户名。</p><p><strong>server-name：</strong>基本上是 github.com。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote add origin git@server-name:path/repo-name.git；</span></span><br></pre></td></tr></table></figure><br><h3 id="推送到-GitHub"><a href="#推送到-GitHub" class="headerlink" title="推送到 GitHub"></a>推送到 GitHub</h3><blockquote><p>把本地库的内容推送到远程，用 <code>git push</code> 命令，实际上是把当前分支 <strong>master</strong> 推送到远程。</p><p>由于远程库是空的，我们第一次推送 <strong>master</strong> 分支时，加上了 <code>-u</code> 参数，Git 不但会把本地的 <strong>master</strong> 分支内容推送的远程新的 <strong>master</strong> 分支，还会把本地的 <strong>master</strong> 分支和远程的 <strong>master</strong> 分支关联起来，在以后的推送或者拉取时就可以简化命令。</p><p><strong>origin：</strong>远程库的名称。</p></blockquote><h4 id="本地仓库推送到远程仓库"><a href="#本地仓库推送到远程仓库" class="headerlink" title="本地仓库推送到远程仓库"></a>本地仓库推送到远程仓库</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push &lt;远程仓库名&gt; &lt;本地分支名&gt;:&lt;远程分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="初次推送"><a href="#初次推送" class="headerlink" title="初次推送"></a>初次推送</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push -u origin master</span></span><br></pre></td></tr></table></figure><h4 id="非初次推送"><a href="#非初次推送" class="headerlink" title="非初次推送"></a>非初次推送</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin master</span></span><br></pre></td></tr></table></figure><br><h3 id="远程库操作"><a href="#远程库操作" class="headerlink" title="远程库操作"></a>远程库操作</h3><h4 id="查看远程库"><a href="#查看远程库" class="headerlink" title="查看远程库"></a>查看远程库</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote -v</span></span><br></pre></td></tr></table></figure><h4 id="删除远程库"><a href="#删除远程库" class="headerlink" title="删除远程库"></a>删除远程库</h4><blockquote><p><strong>注意：</strong>此处的“删除”其实是解除了本地和远程的绑定关系，并不是物理上删除了远程库。远程库本身并没有任何改动。要真正删除远程库，需要登录到 GitHub ，在后台页面找到删除按钮再删除。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git remote rm &lt;远程库名称&gt;</span></span><br></pre></td></tr></table></figure><h4 id="SSH-克隆远程库"><a href="#SSH-克隆远程库" class="headerlink" title="SSH 克隆远程库"></a>SSH 克隆远程库</h4><blockquote><p><strong>repo-name：</strong>克隆仓库名。</p><p><strong>user-name：</strong>克隆仓库的用户名。</p><p><strong>dir：</strong>要克隆到本地的文件目录，默认是”./“。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> git@github.com:user-name/repo-name.git [dir]</span></span><br></pre></td></tr></table></figure><h4 id="HTTPS-克隆远程库"><a href="#HTTPS-克隆远程库" class="headerlink" title="HTTPS 克隆远程库"></a>HTTPS 克隆远程库</h4><blockquote><p><strong>https-link：</strong>克隆仓库的 <code>https</code> 链接。</p><p><strong>dir：</strong>要克隆到本地的文件目录，默认是”./“。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> https-link [dir]</span></span><br></pre></td></tr></table></figure><br><h3 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h3><h4 id="新建分支"><a href="#新建分支" class="headerlink" title="新建分支"></a>新建分支</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch &lt;分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="切换分支"><a href="#切换分支" class="headerlink" title="切换分支"></a>切换分支</h4><blockquote><p><code>-c</code>: 新建分支并切换到该分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git switch [-c] &lt;分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="查看当前分支"><a href="#查看当前分支" class="headerlink" title="查看当前分支"></a>查看当前分支</h4><blockquote><p>当前分支会带”*“号。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch</span></span><br></pre></td></tr></table></figure><h4 id="合并分支"><a href="#合并分支" class="headerlink" title="合并分支"></a>合并分支</h4><blockquote><p><code>git merge</code> 命令用于合并指定分支到当前分支。</p><p><strong>Fast-forward：</strong>合并是“快进模式”，也就是直接把<strong>当前分支</strong>指向<strong>要合并分支</strong>的当前提交，所以合并速度非常快。但这种模式下，删除分支后，会丢掉分支信息。</p><p><code>--no-ff</code>：表示禁用 Fast forward，从分支历史上就可以看出分支信息。</p><p><code>-m</code>：合并要创建一个新的commit。</p><p><strong>分支策略：</strong></p><p>在实际开发中，我们应该按照几个基本原则进行分支管理：</p><p>首先， <code>master</code> 分支应该是非常稳定的，也就是仅用来发布新版本，平时不能在上面干活；</p><p>那在哪干活呢？干活都在 <code>dev</code> 分支上，也就是说， <code>dev</code> 分支是不稳定的，到某个时候，比如1.0版本发布时，再把 <code>dev</code> 分支合并到 <code>master</code> 上，在 <code>master</code> 分支发布1.0版本；</p><p>你和你的小伙伴们每个人都在 <code>dev</code> 分支上干活，每个人都有自己的分支，时不时地往 <code>dev</code> 分支上合并就可以了。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git merge [options] &lt;要合并到当前分支的分支名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="删除分支"><a href="#删除分支" class="headerlink" title="删除分支"></a>删除分支</h4><blockquote><p><code>-d</code>：删除分支。</p><p><code>-D</code>：强制删除，用于没有合并的分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git branch -d/-D &lt;分支名&gt;</span> </span><br></pre></td></tr></table></figure><h4 id="BUG-分支"><a href="#BUG-分支" class="headerlink" title="BUG 分支"></a>BUG 分支</h4><ul><li>封存当前工作现场（封存已跟踪文件）。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash</span></span><br></pre></td></tr></table></figure><ul><li>查看封存的工作现场。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash list</span></span><br></pre></td></tr></table></figure><ul><li><p>恢复封存的工作现场</p><ul><li>用 <code>git stash apply</code> 恢复，但是恢复后，stash内容并不删除，你需要用 <code>git stash drop</code> 来删除。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash apply &lt;恢复编号&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash drop &lt;恢复编号&gt;</span></span><br></pre></td></tr></table></figure><ul><li>用 <code>git stash pop</code> ，恢复的同时把stash内容也删了。</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git stash pop</span></span><br></pre></td></tr></table></figure></li><li><p>复制已有 bug 分支（如果是未提交的工作现场，要先封存或提交，才能复制已有 bug 分支）。</p></li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git cherry-pick &lt;bug 版本号&gt;</span></span><br></pre></td></tr></table></figure><h4 id="多人协作"><a href="#多人协作" class="headerlink" title="多人协作"></a>多人协作</h4><p>因此，多人协作的工作模式通常是这样：</p><ol><li>首先，可以试图用 <code>git push origin &lt;branch-name&gt;</code> 推送自己的修改；</li><li>如果推送失败，则因为远程分支比你的本地更新，需要先用 <code>git pull</code> 试图合并；</li><li>如果合并有冲突，则解决冲突，并在本地提交；</li><li>没有冲突或者解决掉冲突后要 <code>add</code> 和 <code>commit</code> 后，再用 <code>git push origin &lt;branch-name&gt;</code> 推送就能成功！</li></ol><p>如果 <code>git pull</code> 提示 <code>no tracking information</code> ，则说明本地分支和远程分支的链接关系没有创建，用命令 <code>git branch --set-upstream-to &lt;branch-name&gt; origin/&lt;branch-name&gt;</code> 。</p><p>这就是多人协作的工作模式，一旦熟悉了，就非常简单。</p><h4 id="整理分支线"><a href="#整理分支线" class="headerlink" title="整理分支线"></a>整理分支线</h4><blockquote><p><strong>特点：</strong>把分叉的提交历史“整理”成一条直线，看上去更直观。缺点是本地的分叉提交已经被修改过了。</p><p><strong>使用：</strong>本地分支过时与远程分支。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git rebase</span></span><br></pre></td></tr></table></figure><br><h3 id="标签管理"><a href="#标签管理" class="headerlink" title="标签管理"></a>标签管理</h3><h4 id="创建标签"><a href="#创建标签" class="headerlink" title="创建标签"></a>创建标签</h4><blockquote><ul><li>指定分支打标签，首先要切换到指定分支。</li><li><strong>commit id：</strong>指定版本号创建标签。</li><li><strong>options：</strong>用 <code>-a</code> 指定标签名， <code>-m</code> 指定说明文字。</li></ul></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag [options] &lt;标签名&gt; [commit id]</span></span><br></pre></td></tr></table></figure><h4 id="查看标签"><a href="#查看标签" class="headerlink" title="查看标签"></a>查看标签</h4><ul><li>查看所有标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag</span></span><br></pre></td></tr></table></figure><ul><li>查看标签信息</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git show &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="删除标签"><a href="#删除标签" class="headerlink" title="删除标签"></a>删除标签</h4><ul><li>删除标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git tag -d &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>删除远程标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin :refs/tags/&lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><h4 id="推送标签"><a href="#推送标签" class="headerlink" title="推送标签"></a>推送标签</h4><ul><li>推送指定标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin &lt;标签名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>推送全部标签</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git push origin --tags</span></span><br></pre></td></tr></table></figure><br><h3 id="忽略特色文件"><a href="#忽略特色文件" class="headerlink" title="忽略特色文件"></a>忽略特色文件</h3><p>在Git工作区的根目录下创建一个特殊的 <code>.gitignore</code> 文件，然后把要忽略的文件名填进去，Git就会自动忽略这些文件。</p><p>不需要从头写 <code>.gitignore</code> 文件，GitHub 已经为我们准备了各种配置文件，只需要组合一下就可以使用了。所有配置文件可以直接在线浏览：<a href="https://github.com/github/gitignore%E3%80%82">https://github.com/github/gitignore。</a></p><p>忽略文件的原则是：</p><ol><li>忽略操作系统自动生成的文件，比如缩略图等；</li><li>忽略编译生成的中间文件、可执行文件等，也就是如果一个文件是通过另一个文件自动生成的，那自动生成的文件就没必要放进版本库，比如 Java 编译产生的 <code>.class</code> 文件；</li><li>忽略你自己的带有敏感信息的配置文件，比如存放口令的配置文件。</li></ol><p>如果你想添加被忽略的文件，可以用 <code>-f</code> 强制添加到 Git；或者你发现，可能是 <code>.gitignore</code> 写得有问题，需要找出来到底哪个规则写错了，可以用 <code>git check-ignore</code> 命令检查：</p><p>把指定文件排除在 <code>.gitignore</code> 规则外的写法就是 <code>!</code> + 文件名。</p><br><h3 id="配置别名"><a href="#配置别名" class="headerlink" title="配置别名"></a>配置别名</h3><blockquote><p>配置Git的时候，加上 <code>--global</code> 是针对当前用户起作用的，如果不加，那只针对当前的仓库起作用。</p><p>每个仓库的Git配置文件都放在 <code>.git/config</code> 文件中。</p><p>当前用户的Git配置文件放在用户主目录下的一个隐藏文件<code>.gitconfig</code>中。</p></blockquote><ul><li>配置命令</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global <span class="built_in">alias</span>.&lt;别名&gt; &lt;原名&gt;</span></span><br></pre></td></tr></table></figure><ul><li>配置一个 <code>git last</code> ，让其显示最后一次提交信息</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global alias.last <span class="string">&#x27;log -1&#x27;</span></span></span><br></pre></td></tr></table></figure><ul><li>查看日志配置别名</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --global alias.lg &quot;log --color --graph --pretty=format:&#x27;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#x27; --abbrev-commit&quot;</span><br></pre></td></tr></table></figure><br><h3 id="Git-技巧"><a href="#Git-技巧" class="headerlink" title="Git 技巧"></a>Git 技巧</h3><ul><li><p>使用命令 <code>git add &lt;file&gt;</code> ，注意，可反复多次使用，添加多个文件；</p></li><li><p>使用命令 <code>git commit -m &lt;message&gt;</code> ，完成提交文件到本地仓库。</p></li></ul><ul><li><p>要随时掌握工作区的状态，使用 <code>git status</code> 命令。</p></li><li><p>如果 <code>git status</code> 告诉你有文件被修改过，用 <code>git diff</code> 可以查看修改内容。</p></li><li><p> <code>HEAD</code> 指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令 <code>git reset --hard commit_id</code>。</p></li><li><p>穿梭前，用 <code>git log</code> 可以查看提交历史，以便确定要回退到哪个版本。</p></li><li><p>要重返未来，用 <code>git reflog</code> 查看命令历史，以便确定要回到未来的哪个版本。</p></li><li><p>每次修改，如果不用<code>git add</code>到暂存区，那就不会加入到<code>commit</code>中。</p></li><li><p>撤销修改操作：</p><ul><li>场景1：当你改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，用命令 <code>git restore &lt;file&gt;</code>。</li><li>场景2：当你不但改乱了工作区某个文件的内容，还添加到了暂存区时，想丢弃修改，分两步，第一步用命令 <code>git restore --staged &lt;file&gt;</code>，就回到了场景1，第二步按场景1操作。</li><li>场景3：已经提交了不合适的修改到版本库时，想要撤销本次提交，回退版本，不过前提是没有推送到远程库。</li></ul></li><li><p>命令 <code>git rm</code> 用于删除一个文件。如果一个文件已经被提交到版本库，那么你永远不用担心误删，但是要小心，你只能恢复文件到最新版本，你会丢失<strong>最近一次提交后你修改的内容</strong>。</p></li><li><p>建议的删除操作：</p><ol><li><p>用 <code>rm</code> 或者鼠标点击的方式删除文件。</p></li><li><p>用命令 <code>git rm &lt;file&gt;</code> 确认删除文件。</p></li><li><p>用命令 <code>git restore &lt;file&gt;</code> 返回被删除的文件（无论文件修改与否，返回的都是未修改前的文件）。</p></li></ol></li><li><p>要关联一个远程库，使用命令 <code>git remote add origin git@server-name:path/repo-name.git</code> ；</p></li><li><p>关联一个远程库时必须给远程库指定一个名字， <code>origin</code> 是默认习惯命名；</p></li><li><p>关联后，使用命令 <code>git push -u origin master</code> 第一次推送master分支的所有内容；</p></li><li><p>此后，每次本地提交后，只要有必要，就可以使用命令 <code>git push origin master</code> 推送最新修改；</p></li><li><p>要克隆一个仓库，首先必须知道仓库的地址，然后使用<code>git clone</code>命令克隆。</p></li><li><p>Git支持多种协议，包括<code>https</code>，但<code>ssh</code>协议速度最快。</p></li><li><p>Git鼓励大量使用分支：</p><ul><li>查看分支：<code>git branch</code></li><li>创建分支：<code>git branch &lt;name&gt;</code></li><li>切换分支：<code>git checkout &lt;name&gt;</code> 或者 <code>git switch &lt;name&gt;</code></li><li>创建+切换分支：<code>git checkout -b &lt;name&gt;</code> 或者 <code>git switch -c &lt;name&gt;</code></li><li>合并某分支到当前分支：<code>git merge &lt;name&gt;</code></li><li>删除分支：<code>git branch -d &lt;name&gt;</code></li><li>当Git无法自动合并分支时，就必须首先解决冲突。解决冲突后，再提交，合并完成。解决冲突就是把Git合并失败的文件手动编辑为我们希望的内容，再提交。用<code>git log --graph</code>命令可以看到分支合并图。</li></ul></li></ul><ul><li>合并分支时，加上<code>--no-ff</code>参数就可以用普通模式合并，合并后的历史有分支，能看出来曾经做过合并，而<code>fast forward</code> 合并就看不出来曾经做过合并。</li><li>bug 分支<ul><li>修复 bug 时，我们会通过创建新的 bug 分支进行修复，然后合并，最后删除；</li><li>当手头工作没有完成时，先把工作现场 <code>git stash</code> 一下，然后去修复 bug ，修复后，再 <code>git stash pop</code> ，回到工作现场；</li><li>在 master 分支上修复的 bug ，想要合并到当前 dev 分支，可以用 <code>git cherry-pick &lt;commit&gt;</code> 命令，把 bug 提交的修改“复制”到当前分支，避免重复劳动。</li></ul></li><li>开发一个新feature，最好新建一个分支；如果要丢弃一个没有被合并过的分支，可以通过 <code>git branch -D &lt;name&gt;</code> 强行删除。</li><li>多人协作<ul><li>查看远程库信息，使用 <code>git remote -v</code> ；</li><li>本地新建的分支如果不推送到远程，对其他人就是不可见的；</li><li>从本地推送分支，使用 <code>git push origin branch-name</code> ，如果推送失败，先用 <code>git pull</code> 抓取远程的新提交；</li><li>在本地创建和远程分支对应的分支，使用 <code>git switch -c branch-name origin/branch-name</code> ，本地和远程分支的名称最好一致；</li><li>建立本地分支和远程分支的关联，使用 <code>git branch --set-upstream branch-name origin/branch-name</code> ；</li><li>从远程抓取分支，使用 <code>git pull</code> ，如果有冲突，要先处理冲突。</li></ul></li><li><code>rebase</code> 操作可以把本地未 push 的分叉提交历史整理成直线；<code>rebase</code> 的目的是使得我们在查看历史提交的变化时更容易，因为分叉的提交需要三方对比。</li><li>标签管理<ul><li>命令 <code>git tag &lt;tagname&gt;</code> 用于新建一个标签，默认为 <code>HEAD</code> ，也可以指定一个 commit id；</li><li>命令 <code>git tag -a &lt;tagname&gt; -m &quot;blablabla...&quot;</code> 可以指定标签信息；</li><li>命令 <code>git tag</code> 可以查看所有标签；</li><li>命令 <code>git push origin &lt;tagname&gt;</code> 可以推送一个本地标签；</li><li>命令 <code>git push origin --tags</code> 可以推送全部未推送过的本地标签；</li><li>命令 <code>git tag -d &lt;tagname&gt;</code> 可以删除一个本地标签；</li><li>命令 <code>git push origin :refs/tags/&lt;tagname&gt;</code> 可以删除一个远程标签。</li></ul></li><li>在GitHub上，可以任意Fork开源仓库；自己拥有Fork后的仓库的读写权限；可以推送pull request给官方仓库来贡献代码。</li><li>忽略某些文件时，需要编写 <code>.gitignore</code> ；<code>.gitignore</code> 文件本身要放到版本库里，并且可以对 <code>.gitignore</code> 做版本管理！</li></ul><br><br><p><strong>对本文中的不解或本文中存在的错误，欢迎在下方评论区指出！</strong></p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo yilia 图片无法显示问题</title>
      <link href="/archives/fbe14a8f.html"/>
      <url>/archives/fbe14a8f.html</url>
      
        <content type="html"><![CDATA[<h3 id="问题说明"><a href="#问题说明" class="headerlink" title="问题说明"></a>问题说明</h3><blockquote><p>关于 Hexo yilia 的图片无法正常显示的问题，从网上总结出三种解决方案：</p><ul><li>第一种方案将图片放置在 blog/source/image/（博客根目录）下，会导致图片在文章展开时无法显示。（无法解决问题）</li><li>第二种方案是通过下载 hexo-asset-image 来解决问题，图片依旧无法显示。（无法解决问题）</li><li>第三种方案是通过下载 <a href="https://github.com/CodeFalling/hexo-asset-image">https://github.com/CodeFalling/hexo-asset-image</a> 来解决问题，图片可以正常显示。（可以解决问题）</li></ul></blockquote><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><ul><li>将博客根目录下的 _config.yml 中的 <code>post _asset_folder: false</code> 改为：</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">post_asset_folder:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><ul><li>下载 hexo-asset-image</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">npm install https://github.com/CodeFalling/hexo-asset-image</span><br><span class="line"><span class="meta">#</span><span class="bash"> 或者</span></span><br><span class="line">cnpm install https://github.com/CodeFalling/hexo-asset-image</span><br></pre></td></tr></table></figure><ul><li>新建文章</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage1.png" alt="新建文章"></p><ul><li>会出现和文章同名的文件夹，把图片素材放到此文件夹</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage2.png" alt="同名文件夹"></p><ul><li>最后在 .md 文件中插入图片即可正常显示</li></ul><blockquote><p><strong>注意：</strong>插入的图片路径要用相对路径</p></blockquote><ul><li>最后再清理、部署、启动服务器即可</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure><br><h3 id="其他问题"><a href="#其他问题" class="headerlink" title="其他问题"></a>其他问题</h3><ol><li>设置路径时，应当用 “./hexoimage/test.jpg” 而不是 “.\hexoimage\test.jpg”。虽然在 Typora 上图片都能正常显示，但是部署到服务器会导致图片名称乱码，因而无法正常显示。</li><li>尽量不要使用 “hexoimage/test.jpg” 或 “/hexoimage/test.jpg” 虽然在 Typora 上都能正常显示，但是部署到服务器可能会出现问题。</li></ol><p><strong>例如：</strong></p><ul><li>下图中的 “.%5Chexoimage%5Ctest.jpg” 就是乱码的 “test.jpg”</li></ul><p><img src="https://cdn.jsdelivr.net/gh/hellovie/picgo/img/hexoImage3.png" alt="乱码路径"></p><br><br><p><strong>最后：希望本文能够帮助你解决问题，文章中有不懂或不正确的地方，请在下方评论区说明！</strong></p>]]></content>
      
      
      <categories>
          
          <category> 疑难杂症 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Hexo </tag>
            
            <tag> Yilia </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>欢迎来到我的博客</title>
      <link href="/archives/b250e9bb.html"/>
      <url>/archives/b250e9bb.html</url>
      
        <content type="html"><![CDATA[<p><strong>欢迎来到我的个人博客</strong></p><p><font color="#555555">我将在这里记录：</font></p><ul><li><font color="#555555">我的学习笔记</font></li><li><font color="#555555">我的学习项目经验</font></li><li><font color="#555555">我学习过程中遇到的问题</font></li></ul><p><font color="#555555">希望在提高自己的同时，也能对你有所帮助。</font></p><br><p><font color="#555555">欢迎大家留言，指正我的错误</font></p><p><font color="#555555">或者通过邮箱互相交流学习经验</font></p>]]></content>
      
      
      <categories>
          
          <category> 日常分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 关于我 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
